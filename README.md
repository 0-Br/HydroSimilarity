# HydroSimilarity

水文相似性指标体系的构建研究 | Construction of Hydrological Similarity Index System

清华大学杨大文老师《水文学》课程研究项目。

## 研究背景

在水文学中，"水文相似性"是一个核心问题：给定两个流域，如何量化判断它们在水文行为上是否相似？这对于无资料地区的水文预报（PUB, Prediction in Ungauged Basins）具有重要意义——如果我们能找到与目标流域"水文相似"的已观测流域，就可以将后者的水文模型参数迁移过去。

然而，水文相似性难以直接度量。本项目的核心思路是：**用流域的下垫面物理特征（容易获取）来预测水文行为的相似性（难以直接度量）**，从而建立一套可操作的水文相似性指标体系。

## 方法

### 数据

项目使用了 25 个子流域的数据（编号 1–25，缺第 18 号），每个流域包含：

- **下垫面特征（14 维）**：平均高程、排水密度、坡度、TWI（地形湿度指数）、粘土/砂/粉砂含量、NDVI 趋势及四季值、耕地与城市用地比例
- **水文特征（5 维）**：径流系数、基流系数、退水系数（early / custom / late）

数据经 MinMax 归一化后用于建模。

### 相似性度量

定义了四种逐元素相似性函数，将两个特征向量映射为一个相似性向量：

| 函数 | 公式 | 特点 |
|------|------|------|
| quotient | min(a,b) / max(a,b) | 商形式，对量级差异敏感 |
| delta | 1 − \|a − b\| | 差形式，线性衰减 |
| exponent | exp(−(a−b)²) | 指数形式，高斯核风格 |
| quadratic | 1 − (a−b)² | 距离形式，二次衰减 |

### 建模

使用正则化线性回归（Ridge / Lasso）建立下垫面相似性向量到水文相似性向量的映射：

1. 对训练集中所有流域对，计算下垫面相似性向量 **X**（14 维）和水文相似性向量 **Y**（5 维）
2. 用 Ridge 或 Lasso 回归拟合 X → Y
3. 通过 Lasso 路径分析识别对各水文特征最显著的下垫面因子（主成分分析）
4. 对预测结果进行三级相似性评级（高度相似 / 大致相似 / 不相似）

### 评估

将 24 个流域按编号分为训练集（12 个）和验证集（12 个），对比四种相似性函数在 Ridge 回归下的预测表现，并通过回归系数热力图进行显著性分析。

## 项目结构

```
HydroSimilarity/
├── watershed.py      # Watershed 数据类：加载流域属性，提取特征向量
├── similarity.py     # 四种相似性度量函数
├── model.py          # SimilarityModel：训练、预测、评级
├── report.ipynb      # 完整实验流程：预处理→PCA→模型评价→显著性分析
├── data/             # 原始数据（Excel）
└── cache/            # 预处理后的流域对象（pickle）
```

## 评价

这是一个结构清晰、思路完整的课程研究项目。几个值得注意的亮点：

- **问题建模巧妙**：将"水文相似性预测"转化为"从下垫面相似性向量到水文相似性向量的回归问题"，既有物理直觉又便于量化分析
- **对比设计合理**：四种相似性函数 × Ridge/Lasso 正则化，形成系统的对比实验；Lasso 路径分析天然地实现了特征重要性排序
- **代码风格干净**：`Watershed` 类封装了流域的全部属性与特征提取逻辑，`SimilarityModel` 将相似性函数作为参数注入，使得不同度量方式的切换非常自然

项目展示了利用机器学习方法解决传统水文学问题的一种务实路径。
